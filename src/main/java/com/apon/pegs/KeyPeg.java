package com.apon.pegs;

/**
 * Enum values representing the different key pegs.
 */
public enum KeyPeg {
    /** Peg representing the correct color in the correct location. */
    WHITE,
    /** Peg representing the correct color in the wrong location. */
    BLACK,
    /** Value representing an empty peg, meaning incorrect color. */
    NONE
}
